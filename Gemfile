source "https://rubygems.org"

# Keep in sync with GitHub Actions workflow (ruby-version). Ruby itself is managed by the runner.

gem "jekyll", "~> 4.3"

gem "minima", "~> 2.5"

group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.17"
  gem "jekyll-seo-tag", "~> 2.8"
end

# Needed for Ruby 3+ when running `jekyll serve` locally.
# (Not strictly required for `jekyll build`, but harmless.)
gem "webrick", "~> 1.8"
